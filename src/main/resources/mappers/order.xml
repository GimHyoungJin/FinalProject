<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.movio.order.OrderMapper">

	<select id="totalamount" parameterType="string" resultType="int">
	    SELECT SUM(order_amount) FROM orders WHERE s_id = #{s_id}
	</select>

    <insert id="orderInsert" parameterType="kr.co.movio.order.OrderDTO">
        INSERT INTO orders (order_no, s_id, order_date, total_amount)
        VALUES (#{order_no}, #{s_id}, #{order_date}, #{total_amount})
    </insert>

    <insert id="orderdetailInsert" parameterType="map">
        INSERT INTO orderdetail (order_no, pro_detail_code, order_detail_amount)
        VALUES (#{order_no}, #{pro_detail_code}, #{order_detail_amount})
    </insert>

    <delete id="cartDelete" parameterType="string">
        DELETE FROM cart WHERE s_id = #{s_id}
    </delete>

    <select id="orderDesc" resultType="hashmap" parameterType="string">
        SELECT pro_detail_code, order_detail_amount AS quantity
        FROM orderdetail
        WHERE order_no = #{orderNo}
    </select>

</mapper>